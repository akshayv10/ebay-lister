# No third-party dependencies — the whole pipeline uses the Python 3.11 standard
# library only (urllib, hmac, smtplib, zoneinfo, etc.). This file exists so the
# workflow's `pip install -r requirements.txt` is a no-op if you add deps later.
